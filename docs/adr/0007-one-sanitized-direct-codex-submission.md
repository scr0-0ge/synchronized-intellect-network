---
status: accepted
---

# Add one sanitized Direct Project Command seam to the Workbench

After durable `ProjectChannel.act()` acceptance and live Project observation were accepted, the first production mutation will be one direct-input composer that starts one new Codex Agent Session. The renderer Interface gains exactly `submitDirectInput(input)` beside `observeProject(listener)`; the deep Workbench backend Module owns input validation, command identity, the fixed initial `gpt-5.6-sol / ultra / single-agent / full-access` Session Profile, durable submission, and fixed public result mapping. Production injects the accepted `CodexAdapter`, but opening or observing a Project remains runtime-free and official Codex continues to own authentication and execution.

Electron is only a fixed-channel Adapter with owning-sender validation. The renderer learns whether its input was durably accepted or rejected through fixed public copy, while catalog data, command/session/native identifiers, filesystem paths, credential facts, and native payloads remain behind the seam. The approved A desktop hierarchy and B narrow-screen context strip gain the same bounded composer; model/profile controls, resume, steering, interruption, Claude, raw-input persistence, cross-process writers, multiple Projects, and Supervisor automation remain deferred.
